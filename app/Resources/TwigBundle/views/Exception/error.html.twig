{% extends 'base.html.twig' %}

{% block title %} Error {{ status_code }}{% endblock %}

{% block body %}
    <div class="error container col-md-6 col-md-offset-3">
        <div class="logo-error">
            <i class="fa fa-tv fa-4x"> kveo</i>
        </div>
        <hr>
        <i class="fa fa-frown-o fa-5x"></i>
        <h3> {{ 'error.generic'|trans({}, 'messages') }}
            {{ status_code  }}
            <br>
            {{ exception.message }}
            <br>
            <a href="{{ path('home') }}">{{ 'error.back_home'|trans({}, 'messages') }}</a>
        </h3>
    </div>
{% endblock %}
