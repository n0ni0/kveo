<form method="POST" id="commentForm" action="{{path('comment_create', {'media': media.id})}}">
    {{ form_row(form.comment) }}
    {{form_rest(form)}}
    <button type="submit" id="submitButton" class="btn btn-xs btn-danger">{{ 'btn.send' |trans }}</button>
</form>